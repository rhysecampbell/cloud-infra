exports.config = {
  nodeName: "{{ ansible_nodename }}",
  logStreams: {
{% for stream in logstreams %}
    {{ stream.streamname }}: [ "{{ stream.logfile }}" ],
{% endfor %}
  },
  server: {
    host: '0.0.0.0',
    port: 38777
  }
}
